name: test-workflow
on: [push]
jobs:
  check-bats-version:
    runs-on: debian-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
      - run: npm i -g bats
      - run: bats -v
